# Override this ONLY for local development if the backend is on a different port than the frontend.
# Leave blank or remove for production (the app will auto-connect to window.location.origin).
VITE_BACKEND_URL=http://localhost:4001
